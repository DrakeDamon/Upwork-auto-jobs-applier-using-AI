{
  "title": "My Actor Inputs",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchQuery": {
      "title": "Search Query",
      "type": "string",
      "description": "Enter the term to search for."
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "default": 10,
      "minimum": 1,
      "maximum": 100,
      "description": "Maximum number of results to return."
    }
  },
  "required": ["searchQuery"]
}
